package com.example.plateful.ui.util

/**
 * Enum class representing different types of navigation used within the Plateful app.
 */
enum class NavigationType {
    BOTTOM_NAVIGATION,
    NAVIGATION_RAIL,
    PERMANENT_NAVIGATION_DRAWER,
}
